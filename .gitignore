/ tmp
/out-tsc

/node_modules
**/node_modules
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.pnp
.pnp.js

/dist
/coverage
/.tmp
/data

.vscode/*
!.vscode/settings.json
!.vscode/extensions.json

.idea/
.DS_Store
.env
.env.local
.env.*.local
# Sprint 7.3 — production env (uploaded to server, never committed).
# Generic catch-all for any future per-env file too (.env.staging, etc.).
.env.production
.env.staging
.env.*.production
.claude/

# Visual-diff transient run output — gold files in tests/visual-diff-gold/
# ARE committed and reviewed. Per-run rendered PNGs + diff overlays are
# not (they regenerate every `npm run visual-diff`).
tests/visual-diff-output/
