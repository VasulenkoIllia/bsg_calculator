# Critical Audit Report (Code + Docs)

Date: 2026-05-01  
Scope: `src/*`, `docs/*`, `README.md`  
Audit mode: architecture + maintainability + regression risk (no business-logic changes)

## 1. Executive summary

- Runtime is stable after refactor and cleanup.
- Business formulas and UI behavior are preserved.
- Main remaining risk is maintainability of two very large modules (`useCalculatorDerivedData`, Zone 3/4 UI files).

## 2. Verification evidence

Executed on current code:

```bash
npm run typecheck
npm test
npm run build
```

Result: all checks passed.

## 3. Remediation completed in this cycle

### R1 — Numeric parser edge case fixed

Files:
- `src/components/calculator/numberUtils.ts`
- `src/components/calculator/numberUtils.test.ts`

What changed:
- `parseInputNumber` now correctly handles dot-grouped thousands (`1.000`, `10.000`, `1.000.000`).
- Existing support for comma-decimals and mixed separators remains.

Impact:
- Prevents silent value corruption for locale-style input with dot thousands.
- No change to formula business rules.

### R2 — Dead ambiguity plumbing removed

Files:
- `src/App.tsx`
- `src/components/calculator/useCalculatorDerivedData.ts`
- `src/components/calculator/cards.tsx`
- `src/components/calculator/index.ts`

What changed:
- Removed unused ambiguity flags and prop wiring.
- Removed obsolete `SpecAmbiguityNotice` component/export.

Impact:
- Reduced prop surface and dead branches.
- No runtime behavior change (flags were hardcoded `false`).

### R3 — App integration tests decomposed by area

Files:
- removed: `src/App.test.tsx`
- added:
  - `src/test/renderApp.tsx`
  - `src/test/app.core.test.tsx`
  - `src/test/app.zone2.test.tsx`
  - `src/test/app.zone3-zone4.test.tsx`
  - `src/test/app.zone5-zone6.test.tsx`

What changed:
- Split monolithic UI suite into smaller focused files.
- Kept existing user flows/assertions intact.

Impact:
- Better readability and easier regression triage.
- Same functional coverage maintained.

## 4. Findings still open (priority ordered)

### P1 — `useCalculatorDerivedData` remains too large (partially mitigated)

File: `src/components/calculator/useCalculatorDerivedData.ts`  
Size (after 2026-05-02 extraction): ~900 lines

Progress:
- Unified profitability tree construction was extracted to:
  - `src/components/calculator/derived/buildUnifiedProfitabilityTree.ts`
- Unified tree expansion state management was extracted to:
  - `src/components/calculator/derived/useUnifiedTreeExpansion.ts`
- `useCalculatorDerivedData` now focuses on deriving calculation inputs and wiring.

Risk:
- High change blast radius and review complexity.

Recommendation:
- Continue splitting into focused modules:
  - core derived financials by domain segment,
  - unified expansion controls.

### P2 — Zone 3/4 UI files are still large

Files:
- `src/components/calculator/zones/Zone3PricingConfiguration.tsx`
- `src/components/calculator/zones/Zone4OtherFeesAndLimits.tsx`

Risk:
- UI edits are hard to isolate and verify.

Recommendation:
- Extract local subcomponents per logical section (cards/forms/breakdown blocks) without changing formulas.

### P3 — Quality gate gap (no lint stage)

Files:
- `package.json`
- `.github/workflows/ci.yml`

Risk:
- Style and some static issues can slip in because only type/test/build are enforced.

Recommendation:
- Add `lint` script and include it in `npm run verify` + CI.

## 5. Regression status

- No business-logic changes introduced in this cycle.
- No intentional UI/UX behavior changes beyond dead-code cleanup.
- Verified green on typecheck/tests/build.
