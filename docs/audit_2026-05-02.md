# Critical Audit Report (Code + Docs)

Date: 2026-05-02
Scope: full repo, focused on stabilization before backend phase
Predecessor: `docs/archive/audit_2026-05-01.md`

## 1. Executive summary

The project is stable and ready to enter a "polish & lock" stretch before backend work begins.

- Calculator: working, frozen by user agreement, tests green (151/151).
- Wizard / OFFER PDF: full Stage 1A–1D + Stage 2A landed; supports calculator / manualBlank / manualDefaults sources with one shared renderer.
- Build/typecheck/test: green.
- Major P1 items from previous audit (large file decomposition) closed per `decisions.md` 2026-05-02 entry.

Remaining risks are documentation hygiene and a few code clusters that should be split before AGREEMENT/DOCX or backend work touches them.

## 2. Verification evidence

```
npm run typecheck   ✓
npm run test        ✓ 151/151
npm run build       ✓ 86 modules, 390 KB JS / 21 KB CSS
```

## 3. Cleanup completed in this cycle

### C1 — Root backup docx removed
- Removed: `Calculator_Описание_backup_before_red_marks.docx` (58K, dead backup).

### C2 — Package metadata
- `package.json` `name` updated from `workflo-fullstack-template` to `bsg-calculator`.

### C3 — Documentation archive
Moved to `docs/archive/`:
- `phase_01_zone0_zone1_handoff.md`
- `phase_02_zone2_plan.md`
- `phase_03_zone3_handoff.md`
- `phase_04_zone4_handoff.md`
- `phase_05_zone5_handoff.md`
- `phase_06_zone6_handoff.md`
- `calculator_spec_open_questions.md` (closed, historical only)
- `calculator_delivery_contract.md` (predates Phase 7 model)
- `audit_2026-05-01.md` (superseded by this file)

### C4 — New architecture-level documentation
Added:
- `docs/architecture.md` — module map, data flows, hard rules.
- `docs/spec_v2_alignment.md` — section-by-section status of `technical_specification_bsg.docx v2.0`.
- `docs/integrations.md` — refreshed with HubSpot planned interaction shape (no implementation).

### C5 — Calculator frozen rule pinned
- README and `AGENTS.md` now state: calculator math/business logic must not change without explicit user approval.

## 4. Findings closed in this cycle (P1–P5)

All five decomposition / hygiene items below were completed in a single follow-up pass on 2026-05-02 with the user's explicit approval. The calculator math was untouched: zone5 split is a pure barrel-and-internal refactor; final JS bundle hash is identical to the pre-split build (`index-BVl0XQ7I.js`, 390.57 kB).

Verification after the pass: `typecheck` ✓, `lint` ✓ (0 errors, 3 pre-existing warnings), `test` ✓ 151/151, `build` ✓.

### ✅ P1 — `buildOfferPdfHtml.ts` decomposition
Done. New layout under `src/components/document-wizard/offerPdf/`:
- `formatters.ts` (≈80 lines) — money/percent/date helpers + value-mode resolver
- `layoutResolution.ts` (≈40 lines) — calculator-mode layout fallback
- `sections/payin.ts`, `sections/payout.ts`, `sections/fees.ts`, `sections/terms.ts`
The original `buildOfferPdfHtml.ts` is now a thin orchestrator (~80 lines). Public entry signature unchanged.

### ✅ P2 — `pdf-kit/primitives.ts` decomposition
Done. New layout under `src/components/document-wizard/pdf-kit/`:
- `types.ts` — public item types
- `components/metaItem.ts`, `components/sectionHeader.ts`, `components/feesGrid.ts`, `components/termsGrid.ts`, `components/footer.ts`
- `styles.ts` — `buildPdfUiKitStyles`
- `primitives.ts` is now a barrel re-export, so consumers' imports remain unchanged.

### ✅ P3 — `zone5/profitability.ts` decomposition
Done. **No formula or logic change.** New layout under `src/domain/calculator/zone5/`:
- `types.ts` — all public type definitions
- `constants.ts` — provider tier defaults
- `internals.ts` — shared math helpers (safe parsing, tier splitting, MDR rows, payout TRX rows)
- `payin.ts` — `calculatePayinRegionProfitability` + `calculatePayinProfitability`
- `payout.ts` — `calculatePayoutProfitability`
- `other.ts` — `calculateOtherRevenueProfitability`
- `total.ts` — `calculateTotalProfitability`
- `profitability.ts` — barrel re-export only.
All test outputs identical (151/151 green).

### ✅ P4 — ESLint added
Done. Stack: `eslint@^9`, `@eslint/js@^9`, `typescript-eslint@^8`, `eslint-plugin-react@^7`, `eslint-plugin-react-hooks@^7`, `globals@^17`.
- Flat config in `eslint.config.js`.
- `npm run lint` script and `lint` step wired into `npm run verify` and `.github/workflows/ci.yml`.
- Ignores: `dist/**`, `node_modules/**`, `coverage/**`, `.claude/**` (worktrees), `server/**` (skeleton-only).
- Current state: 0 errors. 3 pre-existing warnings (one in calculator hook — left untouched per "calculator frozen" rule; two in wizard layer — left as warnings, non-blocking).

### ✅ P5 — Wizard payload type renamed to `DocumentTemplatePayload`
Done. `DocumentWizardTemplateData` → `DocumentTemplatePayload` and corresponding `buildDocument…` helpers across 13 files. `BuildDocumentWizardTemplateInput` → `BuildDocumentTemplatePayloadInput`. Naming now matches the phase-07 backend plan.

## 5. Remaining open findings

### P6 — Document number placeholder
File: `src/components/document-wizard/fromCalculator.ts` (`defaultDraftNumber`)
Status: open.
Recommendation: add a single-line comment marking the placeholder origin and the replacement target (Phase 8 numbering service). Not done in this pass to avoid touching wizard helpers without a clear product trigger.

### Pre-existing lint warnings (3)
- `src/App.tsx:376` — `useEffect` missing dep `setOfferSummaryActionMessage` (set-state identity is stable; common pattern).
- `src/components/calculator/useCalculatorDerivedData.ts:369` — `useMemo` unnecessary dep `introducerCommissionAmount` (calculator hook — frozen).
- `src/components/document-wizard/fromCalculator.ts:107` — unused `payout` arg.
None block CI; deferred to a future targeted pass with product approval.

## 5. Regression status

- No business-logic changes introduced.
- No UI/UX behavior changes.
- All formula files in `src/domain/calculator/**` untouched.
- Documentation reorganization is a file-move + new-file cycle; no source code refactor.

## 6. Recommended next steps

1. Close P6 with a comment-only edit (cheap).
2. Lock OFFER renderer outputs against approved samples in a final visual pass.
3. Discuss any remaining frontend-PDF questions before opening the backend phase.
4. When backend phase opens, the canonical payload contract is already aligned (`DocumentTemplatePayload`).
