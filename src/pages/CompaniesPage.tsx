/**
 * Companies listing page — full implementation in Sprint 2.8.D.
 *
 * This placeholder exists so App.tsx routes compile after 2.8.C.
 */

export function CompaniesPage() {
  return (
    <section className="panel border border-slate-200 bg-white p-6">
      <h1 className="text-lg font-semibold text-slate-900">Companies</h1>
      <p className="mt-2 text-sm text-slate-500">
        Listing UI lands in Sprint 2.8.D — wired to GET /api/v1/companies.
      </p>
    </section>
  );
}
